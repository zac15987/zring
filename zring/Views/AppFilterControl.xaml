<UserControl x:Class="Zring.Views.AppFilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ignore="http://dummy"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewModel="clr-namespace:Zring.ViewModel"
             mc:Ignorable="d ignore"
             x:Name="Control"
             d:DataContext="{d:DesignInstance Type=viewModel:AppFilterViewModel}"
             Background="Transparent"
             Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
    <UserControl.DataContext>
        <Binding Source="{StaticResource ViewModelLocator}" Path="AppFilterViewModel" />
    </UserControl.DataContext>

    <Grid>
        <ToggleButton x:Name="FilterToggle"
                      Margin="3,1"
                      Width="{Binding Settings.AppBarButtonHeight}"
                      Height="{Binding Settings.AppBarButtonHeight}"
                      Padding="0"
                      IsChecked="{Binding IsPopupOpen, Mode=TwoWay}"
                      ToolTip="{DynamicResource StrAppFilterPopupTitle}">
            <ui:SymbolIcon Symbol="Filter24" />
        </ToggleButton>

        <Popup x:Name="FilterPopup"
               IsOpen="{Binding IsPopupOpen, Mode=TwoWay}"
               PlacementTarget="{Binding ElementName=FilterToggle}"
               StaysOpen="False"
               AllowsTransparency="True"
               Width="300"
               MaxHeight="450"
               Opened="FilterPopup_OnOpened">
            <Popup.Style>
                <Style TargetType="{x:Type Popup}">
                    <Setter Property="HorizontalOffset" Value="0" />
                    <Setter Property="VerticalOffset" Value="0" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=DockMode, ElementName=Control}" Value="Left">
                            <Setter Property="Placement" Value="Right" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=DockMode, ElementName=Control}" Value="Right">
                            <Setter Property="Placement" Value="Left" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=DockMode, ElementName=Control}" Value="Top">
                            <Setter Property="Placement" Value="Bottom" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=DockMode, ElementName=Control}" Value="Bottom">
                            <Setter Property="Placement" Value="Top" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Popup.Style>
            <Border BorderThickness="1"
                    BorderBrush="{ui:ThemeResource ControlElevationBorderBrush}"
                    Background="{ui:ThemeResource ApplicationBackgroundBrush}">
                <DockPanel Margin="8" LastChildFill="True">
                    <!-- Header -->
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"
                                   Text="{DynamicResource StrAppFilterPopupTitle}" />
                        <Button Grid.Column="1"
                                Padding="8,2"
                                Command="{Binding ClearAllCommand}"
                                Content="{DynamicResource StrAppFilterClearAll}" />
                    </Grid>

                    <!-- Empty state -->
                    <TextBlock DockPanel.Dock="Top"
                               Margin="0,8"
                               HorizontalAlignment="Center"
                               Opacity="0.7"
                               Text="{DynamicResource StrAppFilterEmpty}">
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding RunningApps.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- List -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding RunningApps}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Margin="0,2"
                                              IsChecked="{Binding IsChecked, Mode=TwoWay}">
                                        <StackPanel Orientation="Horizontal">
                                            <Image Source="{Binding Icon}"
                                                   Width="16" Height="16"
                                                   Margin="0,0,6,0"
                                                   VerticalAlignment="Center" />
                                            <TextBlock VerticalAlignment="Center"
                                                       Text="{Binding DisplayName}"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxWidth="200" />
                                            <TextBlock VerticalAlignment="Center"
                                                       Opacity="0.6"
                                                       Text="{Binding WindowCount, StringFormat=' ({0})'}" />
                                        </StackPanel>
                                    </CheckBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>
